[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "py-eas"
version = "0.1.0"
description = "A Python client for Exchange ActiveSync (EAS / MS-ASCMD / MS-ASWBXML)"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Martin Monperrus" }]
requires-python = ">=3.10"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Communications :: Email",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]
dependencies = [
    "requests>=2.28",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "mypy>=1.8",
    "ruff>=0.4",
    "types-requests",
]

[project.urls]
Homepage = "https://github.com/monperrus/py-eas"
Repository = "https://github.com/monperrus/py-eas"
Issues = "https://github.com/monperrus/py-eas/issues"

[project.scripts]
py-eas = "py_eas.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["py_eas"]

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.10"
strict = true

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.coverage.run]
source = ["py_eas"]
